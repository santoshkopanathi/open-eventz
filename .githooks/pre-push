#!/bin/sh
# Pre-push gate: E2E smoke (Playwright) — heavier, so it runs on push rather than every commit.
cd "$(git rev-parse --show-toplevel)" || exit 1
echo "▶ pre-push: E2E smoke (Playwright)"
npm run test:e2e
